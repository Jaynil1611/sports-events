import React from "react";
import { SpinnerContainer, StyledSpinner } from "./styles";

export const Spinner = () => {
  return (
    <SpinnerContainer>
      <StyledSpinner />
    </SpinnerContainer>
  );
};
